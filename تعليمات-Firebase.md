# تفعيل الحفظ السحابي ولوحة الإدارة (Firebase)

هذا الملف يشرح خطوات تفعيل تسجيل الدخول والحفظ التلقائي في السحابة لتطبيق «قصة سامي — A1»، ولوحة الإدارة التي تتيح لك مشاهدة بيانات كل المستخدمين. الخدمة (Firebase) مجانية بالكامل ضمن الخطة الافتراضية (Spark) لهذا الحجم من الاستخدام، ولن تُطلب منك بطاقة دفع.

## الخطوة ١ — إنشاء مشروع Firebase

اذهب إلى console.firebase.google.com وسجّل الدخول بحساب Google الخاص بك (يفضّل ali078044@gmail.com لأنه سيكون حساب المشرف). اضغط "Add project"، أعطِ المشروع اسماً مثل sami-a1، ويمكنك تعطيل Google Analytics إن ظهر خياره لأنه غير ضروري هنا.

## الخطوة ٢ — تفعيل تسجيل الدخول

من القائمة الجانبية اذهب إلى Build ثم Authentication ثم اضغط Get started. افتح تبويب Sign-in method وفعّل كلاً من Google و Email/Password (اضغط على كل واحدة، بدّل المفتاح إلى Enable، ثم Save).

## الخطوة ٣ — إنشاء قاعدة بيانات Firestore

من القائمة الجانبية اذهب إلى Build ثم Firestore Database واضغط Create database. اختر Start in production mode، ثم اختر أقرب منطقة جغرافية لك (مثلاً eur3 أو أي منطقة قريبة)، واضغط Enable.

## الخطوة ٤ — لصق قواعد الأمان

داخل Firestore Database افتح تبويب Rules. احذف المحتوى الموجود، وافتح الملف المرفق «قواعد-أمان-Firestore.txt» وانسخ محتواه بالكامل والصقه مكان القواعد الافتراضية، ثم اضغط Publish. هذه القواعد تضمن أن كل مستخدم يرى بياناته فقط، بينما بريدك (المدرج كمشرف) يمكنه قراءة بيانات الجميع لعرضها في لوحة الإدارة.

## الخطوة ٥ — الحصول على مفاتيح الربط (Config)

من أعلى يسار الصفحة اضغط أيقونة الترس ⚙️ بجانب "Project Overview" ثم اختر Project settings. انزل إلى قسم "Your apps" واضغط على أيقونة الويب </> لإضافة تطبيق ويب. أعطه اسماً (مثلاً sami-web)، لا حاجة لتفعيل Firebase Hosting، ثم اضغط Register app. ستظهر لك قيم مثل:

```
apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId
```

انسخ هذه القيم كاملة (ستحتاجها في الخطوة التالية).

## الخطوة ٦ — لصق القيم داخل ملف التطبيق

افتح ملف index.html (أو الملف الرئيسي للتطبيق) بأي محرر نصوص، وابحث (Ctrl+F) عن العبارة FIREBASE_CONFIG. ستجد كائناً فارغاً كهذا:

```js
const FIREBASE_CONFIG={
  apiKey:"", authDomain:"", projectId:"", storageBucket:"", messagingSenderId:"", appId:""
};
```

الصق القيم التي نسخّتها بين علامتي التنصيص لكل حقل، ثم احفظ الملف.

## الخطوة ٧ — رفع التطبيق على الإنترنت

تسجيل الدخول بحساب Google تحديداً يحتاج أن يكون الموقع مستضافاً عبر https (لا يعمل بشكل موثوق عند فتح الملف مباشرة من جهازك بصيغة file://). أسهل طريقة مجانية هي GitHub Pages:

1. أنشئ مستودعاً جديداً على github.com.
2. ارفع ملف index.html (وبقية ملفات الحزمة: data، icons، manifest.json، sw.js) إلى المستودع.
3. من إعدادات المستودع Settings ثم Pages، اختر Branch: main والمجلد الرئيسي، ثم Save.
4. بعد دقيقة أو دقيقتين سيظهر رابط بالشكل: https://اسمك.github.io/اسم-المستودع

## الخطوة ٨ — السماح للنطاق بتسجيل الدخول

في Firebase Console اذهب إلى Authentication ثم Settings ثم Authorized domains، وتأكد أن نطاق GitHub Pages الخاص بك (مثل yourname.github.io) مضاف إلى القائمة. أضفه يدوياً إن لم يكن موجوداً.

## الخطوة ٩ — الدخول كمشرف

بريد ali078044@gmail.com مُدرج مسبقاً داخل الكود كحساب مشرف (المتغير ADMIN_EMAILS). سجّل الدخول بهذا البريد من داخل التطبيق (صفحة "حسابي") وسيظهر لك زر "🛠 لوحة الإدارة" يعرض كل المستخدمين المسجّلين وتفاصيل محاولاتهم، مع إمكانية تصدير كل ذلك كملف Excel/CSV.

## ملاحظات مهمة

قبل إتمام هذه الخطوات يعمل التطبيق تماماً كما كان — يحفظ التقدّم محلياً على كل جهاز فقط، بلا أي خلل أو رسائل خطأ. الحفظ السحابي ولوحة الإدارة ميزتان إضافيتان تُفعَّلان تلقائياً بمجرد ضبط القيم في الخطوة ٦. إن واجهتك أي مشكلة في أي خطوة أرسل لي لقطة شاشة من الرسالة وسأساعدك.
